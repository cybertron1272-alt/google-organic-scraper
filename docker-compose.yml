version: "3.8"
services:
  app:
    image: node:18
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install && npm start"
    ports:
      - "3000:3000"
    environment:
      - SERP_API_KEY=${SERP_API_KEY}
